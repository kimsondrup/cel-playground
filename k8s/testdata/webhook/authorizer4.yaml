paths:
groups:
  admissionregistration.k8s.io:
    resources:
      validatingwebhookconfigurations:
        checks:
          # A cluster-scoped resource has no namespace, so the key is "".
          "":
            rbac.my-webhook.example.com:
              breakglass:
                decision: allow
serviceAccounts:
